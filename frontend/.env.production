# Build-time API base for production builds (Vercel).
# NEXT_PUBLIC_* values are inlined into the client bundle, so this is public by
# design, not a secret. A value set in the Vercel dashboard overrides this file.
NEXT_PUBLIC_API_URL=https://zoomeet-api.onrender.com
